/*
 * Daniel Goncalves > 1151452@isep.ipp.pt
 * ARQCP - Turma 2DK
 * 
 * where_exists.h
 */
 
/*
 * Determines if the first string exist within the second.
 * char *str1 - the address of first string.
 * char *str2 - the address of second string.
 * 
 * return the pointer to the beginning of the first (or only) occurence
 * or NULL otherwise.
 */
char* where_exists(char *str1, char *str2);
